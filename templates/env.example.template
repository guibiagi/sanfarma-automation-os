# Nome do Projeto — Variáveis de Ambiente
# Copie para .env e preencha com valores reais.
# ⚠️ NUNCA commite .env com valores reais.
# =============================================================================

# --- PostgreSQL (ERP Sanfarma — somente leitura) ---
DB_HOST=ip.do.banco
DB_PORT=5432
DB_NAME=nome_do_banco
DB_USER=usuario_leitura
DB_PASSWORD=senha

# --- Telegram (notificações) ---
TELEGRAM_BOT_TOKEN=seu_bot_token
TELEGRAM_CHAT_ID=seu_chat_id

# --- API Externa ---
EXTERNAL_API_KEY=sua_api_key
EXTERNAL_API_SECRET=seu_secret

# --- Ambiente ---
ENVIRONMENT=development
LOG_LEVEL=INFO
